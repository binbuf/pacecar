Every time we run the app now Microsoft's Threat Scanner pops up and says it has detected a new threat without specifying any details.

Add some buttons to give auto size the form. One button to give it a small 3x3 grid and a 1x6 grid small as possible without UI looking bad. Then on the button that shows the speces have the form resized so everything fits perfectly without any extra fluff. 

Move CPU RPM to its own sub line

Make all tiles and all info in each tile togglable. 
  - Turn off graphs in favor of simple percentage
  - Turn off percentage
  - Turn off every sub label option for that type

Request elevated admin permissions on Windows when app is starting

